<dx:ThemedWindow x:Class="AutoUpdaterManage.PackageBuilderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
        xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
        Title="更新包发布" Width="980" Height="720" MinWidth="820" MinHeight="600"
        WindowStartupLocation="CenterOwner" ShowIcon="False">
    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="创建增量更新包" FontSize="23" FontWeight="SemiBold"/>
            <TextBlock Text="选择程序根目录和需要更新的文件，自动生成 ZIP、SHA-256 与 manifest.json"
                       Margin="0,5,0,0" Foreground="#667085"/>
        </StackPanel>

        <Border Grid.Row="1" Background="#F6F8FB" CornerRadius="8" Padding="16" Margin="0,18,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="90"/>
                    <ColumnDefinition Width="180"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="程序根目录" VerticalAlignment="Center"/>
                <TextBox x:Name="SourceDirectoryBox" Grid.Column="1" Margin="0,0,8,0" IsReadOnly="True"/>
                <Button Grid.Column="2" Content="选择目录" Click="ChooseSource_Click"/>
                <TextBlock Grid.Column="3" Text="目标版本" Margin="18,0,8,0" VerticalAlignment="Center"/>
                <TextBox x:Name="VersionBox" Grid.Column="4" Text="2.0.0.0"/>

                <TextBlock Grid.Row="1" Text="输出目录" Margin="0,12,0,0" VerticalAlignment="Center"/>
                <TextBox x:Name="OutputDirectoryBox" Grid.Row="1" Grid.Column="1"
                         Margin="0,12,8,0" IsReadOnly="True"/>
                <Button Grid.Row="1" Grid.Column="2" Content="选择目录" Margin="0,12,0,0"
                        Click="ChooseOutput_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" BorderBrush="#E1E6EF" BorderThickness="1" CornerRadius="8">
            <dxg:GridControl x:Name="FilesGrid" ItemsSource="{Binding Files}">
                <dxg:GridControl.View>
                    <dxg:TableView ShowGroupPanel="False" AutoWidth="True"
                                   ShowIndicator="False" NavigationStyle="Cell"/>
                </dxg:GridControl.View>
                <dxg:GridColumn FieldName="IsSelected" Width="55" AllowEditing="True">
                    <dxg:GridColumn.HeaderTemplate>
                        <DataTemplate>
                            <CheckBox HorizontalAlignment="Center" VerticalAlignment="Center"
                                      ToolTip="勾选全选，取消勾选清空"
                                      Checked="HeaderSelectAll_Checked"
                                      Unchecked="HeaderSelectAll_Unchecked"/>
                        </DataTemplate>
                    </dxg:GridColumn.HeaderTemplate>
                    <dxg:GridColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding RowData.Row.IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </DataTemplate>
                    </dxg:GridColumn.CellTemplate>
                </dxg:GridColumn>
                <dxg:GridColumn FieldName="RelativePath" Header="相对路径" Width="400" AllowEditing="False"/>
                <dxg:GridColumn FieldName="SizeText" Header="大小" Width="100" AllowEditing="False"/>
            </dxg:GridControl>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <StackPanel Orientation="Horizontal">
                <Button Content="全选" Click="SelectAll_Click" Margin="0,0,8,0"/>
                <Button Content="反选" Click="InvertSelection_Click"/>
                <TextBlock x:Name="SelectionSummary" Margin="16,0,0,0"
                           VerticalAlignment="Center" Foreground="#667085"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="关闭" Click="Close_Click" Margin="0,0,10,0"/>
                <Button x:Name="BuildButton" Content="生成发布包" Padding="22,10" Click="BuildPackage_Click"
                        Background="#2563EB" Foreground="White"/>
            </StackPanel>
        </Grid>
    </Grid>
</dx:ThemedWindow>
